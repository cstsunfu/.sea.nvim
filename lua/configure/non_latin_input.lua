local plugin = {}

plugin.core = {
    "rlue/vim-barbaric",
    opt = false,

    config = function() -- Specifies code to run after this plugin is loaded
    end,
}

plugin.mapping = function()

end


return plugin
